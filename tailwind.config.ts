import type { Config } from 'tailwindcss'

export default {
  theme: {
    fontFamily: {
      body: ['Kanit', 'sans-serif'],
      accent: ['Instrument Serif', 'serif'],
    },
  },
} satisfies Config
